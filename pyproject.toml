[project]
name = "idsignpost-extractor"
version = "0.1.0"
description = "Extract idsignpost HTML entries into markdown."
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4>=4.12.3",
    "markdownify>=0.12.1",
]

[dependency-groups]
dev = [
    "ruff>=0.14.13",
    "ty>=0.0.12",
]

